<div>
  <div class="show_rides" style="position: relative;">
    <div class="ms-2" style="position: absolute; top: 38px;">
      <%= render "shared/back_btn" %>
    </div>
    <div class="title d-flex justify-content-center">
      <h1 class="mt-2 ms-3" style="font-size: 30px;"><strong>Messages</strong></h1>
      <i class="fa-brands fa-rocketchat chat-icon" style="font-size: 30px; padding-left: 8px;"></i>
    </div>
  </div>
</div>


<% @chatrooms.each do |chatroom| %>
  <div class="card-product">
      <% if chatroom.user2.photo.attached? %>
        <%= cl_image_tag chatroom.user2.photo.key %>
      <% else %>
        <%= image_tag "user.png" %>
      <% end %>
    <div class="card-product-infos">
      <h2>
          <% other_user = chatroom.user1 == current_user ? chatroom.user2 : chatroom.user1%>
          <%= link_to other_user.full_name, chatroom_path(chatroom) %>
      </h2>
    </div>
  </div>
<% end %>
