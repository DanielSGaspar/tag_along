

<%= simple_form_for [@booking, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.number_field :rating, min: 1, max: 5 %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit "Submit Review" %>
<% end %>
